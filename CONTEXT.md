# CONTEXT

本项目的领域术语表。

## 核心概念

- **定投（Dollar-Cost Averaging / DCA）**：在固定间隔（按月、按周）以固定或可变金额买入同一标的的投资方式。
- **策略（Strategy）**：用户定义的一套完整的定投配置，包含一至多个片段、费率参数、计算窗口。可导出为文件或分享为 URL。
- **片段（Segment）**：策略的组成单元。每个片段定义一段时间内对某个指数的定投规则：开始日期、结束日期、定投频率（月/周）、定投日、定投金额。一个策略的多个片段允许时间重叠、允许在不同时段定投不同指数。
- **计算窗口（Evaluation Window）**：用户指定的收益计算区间。计算开始日期 ≤ 最早片段开始日期，计算结束日期 ≥ 最晚片段结束日期。定投停止后持仓不卖出，持续计算市值至窗口结束。

## 数据

- **全收益指数（Total Return Index）**：指数编制机构发布的将成分股现金分红再投资回报一并计入的指数。本项目用于近似模拟指数基金的复权净值。
- **复权单位净值**：基金将每次分红再投资的效应叠回净值序列后的调整值。是持有真实基金份额的"等效价格"。
- **AU9999**：上海黄金交易所纯度 99.99% 黄金现货的收盘价，本项目用于近似模拟黄金 ETF 价格。
- **顺延（Roll Forward）**：当定投日恰逢非交易日时，订单顺延到下一交易日执行。

## 收益指标

- **期末总市值（Terminal Market Value）**：所有定投累计买入的份额 × 当前全收益指数值 × (1 - 赎回费率)。
- **累计投入总额（Total Cost）**：所有定投支出之和，含申购费。
- **累计收益率（Cumulative Return）**：(期末总市值 - 累计投入总额) / 累计投入总额。
- **XIRR（Extended Internal Rate of Return）**：扩展内部收益率。将每一笔现金流按实际发生日期折现，求解使净现值为零的年化折现率。金融业约定俗成的定投收益衡量标准。

## 费率（可选）

用户自行填写的可选参数，不填默认为 0：
- **申购费**：买入时按比例一次性收取的费用。
- **赎回费**：赎回时按比例一次性收取的费用。
- **管理费**：按年化比例从净值中持续扣除的费用。
