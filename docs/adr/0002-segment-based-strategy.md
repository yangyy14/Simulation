# 采用片段式策略模型

定投策略由若干独立的**片段（Segment）**组成，而非单一连续计划。

**Why**: 支持用户在不同时间段定投不同指数、暂停、调整金额，并支持将所有这些时段的持仓合并计算跨周期收益。多个片段允许时间重叠，每个片段持有不同指数，所有片段的买入记录汇入统一现金流池计算 XIRR。如果不拆分片段，换指数或暂停只能通过事后手动拼接结果——片段模型令这种跨周期操作成为一等公民。
